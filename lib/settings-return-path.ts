// The settings screen is a standalone full-window layout; "Back to app"
// returns to wherever the user was before entering it. AppShell records that
// location here because SettingsShell itself only mounts on /settings routes.
export const SETTINGS_RETURN_PATH_KEY = "astraflow-settings-return-path"
