# Direct packages required by the bundled document skills/runtime.
# Keep these exact; regenerate requirements.lock after every change.
bubblewrap-bin==0.11.0 ; sys_platform == 'linux'
# cryptography 49 no longer ships a macOS Intel wheel. The bundled runtime
# installs binary wheels only, so keep the newest release available on x64.
cryptography==48.0.1
defusedxml==0.7.1
lxml==6.1.1
markitdown[docx,pptx]==0.1.6
openpyxl==3.1.5
pandas==3.0.3
pdf2image==1.17.0
pillow==12.3.0
pypdf[crypto]==6.14.2
pdfplumber==0.11.10
pypdfium2==5.11.0
pytesseract==0.3.13
python-docx==1.2.0
reportlab==5.0.0
ripgrep-bin==15.1.0 ; sys_platform == 'linux'
